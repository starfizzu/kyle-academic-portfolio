NAME: Jhon Nelly D. Mercado 

SCHOOL: Universidad de Dagupan 

NOTE: I can’t upload the node_modules since the 
dependencies are many, so instead of uploading it, 
just use npm install inside the Visual Studio Code 
terminal to install the dependencies